class Solution {
    public boolean isMatch(String s, String p) {
        int p1 = s.length()-1;
        int p2 = p.length()-1;
        while(p1 >= 0 && p2 >= 0){
            
        }


        return true;
    }
}